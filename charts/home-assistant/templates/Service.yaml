apiVersion: v1
kind: Service
metadata:
  name: ha-web
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
spec:
  ports:
  - port: 80
    targetPort: http
    name: http
  selector:
    app: homeassistant
